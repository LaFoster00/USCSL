using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using USCSL;
public class Test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print(USCSL_Library.Test());
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
